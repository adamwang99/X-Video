{
  "name": "xvideo-studio",
  "private": true,
  "version": "1.2.0",
  "description": "AI World URL-to-Video Studio",
  "scripts": {
    "dev": "tauri dev",
    "build": "tauri build",
    "build:mac": "tauri build --target aarch64-apple-darwin"
  },
  "dependencies": {
    "@tauri-apps/api": "^2.0",
    "@tauri-apps/plugin-shell": "^2.0"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.0"
  }
}
